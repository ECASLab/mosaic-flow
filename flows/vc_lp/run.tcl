# Read RTL and constraints/power.upf, then execute the approved VC LP structural,
# power-state and low-power-sequencing checks for the installed release. Return an
# error when an unwaived violation remains.
error "VC LP adapter is not configured for the installed release"
