# Read RTL, elaborate $env(DESIGN_TOP), load constraints/cdc.tcl and execute the
# approved SpyGlass CDC goals for the installed release. Return an error when an
# unwaived crossing remains.
error "SpyGlass CDC adapter is not configured for the installed release"
