# Read RTL, elaborate $env(DESIGN_TOP), load constraints/cdc.tcl and execute the
# approved VC CDC structural and functional checks for the installed release.
# Return an error when an unwaived crossing remains.
error "VC CDC adapter is not configured for the installed release"
