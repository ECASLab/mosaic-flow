# Fully pinned so the cached PyUVM environment is reproducible in CI and locally.
pyuvm==5.0.0
cocotb==2.1.0
find_libpython==0.5.1
pytest==9.1.1
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
